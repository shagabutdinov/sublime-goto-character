// will go to "t" of "toggleClass", then delete to "C" and write "remove"
$('.block').removeClass('active');

// same, but backward
$('.block').removeClass('active');

// select toggleClass with parethesis and copy and paste it
$('.block').toggleClass('active').toggleClass('active');